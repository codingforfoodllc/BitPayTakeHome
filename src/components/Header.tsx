import styled from "styled-components";

const HeaderContent = styled.div`
  background: yellow;
  height: 9rem;
`;

const Header = () => {
  return <HeaderContent>This is the header</HeaderContent>;
};

export default Header;
